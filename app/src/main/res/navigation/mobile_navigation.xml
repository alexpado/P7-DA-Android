<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/mobile_navigation"
        app:startDestination="@+id/nav_home">

    <fragment
            android:id="@+id/nav_home"
            android:name="fr.alexpado.go4lunch.ui.map.MapFragment"
            android:label="@string/menu_map"
            tools:layout="@layout/fragment_map" />

    <fragment
            android:id="@+id/nav_gallery"
            android:name="fr.alexpado.go4lunch.ui.listview.ListViewFragment"
            android:label="@string/menu_list"
            tools:layout="@layout/fragment_listview" />

    <fragment
            android:id="@+id/nav_slideshow"
            android:name="fr.alexpado.go4lunch.ui.workmates.WorkmatesFragment"
            android:label="@string/menu_workmates"
            tools:layout="@layout/fragment_workmates" />
</navigation>